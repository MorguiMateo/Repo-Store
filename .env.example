# Food Store — Storefront (Repo-Store)  ·  copiar a .env
#   cp .env.example .env

# URL base de la API del backend. DEBE incluir el prefijo /api/v1.
# Si no se define, el front usa este mismo valor por defecto.
VITE_API_URL=http://localhost:8000/api/v1
